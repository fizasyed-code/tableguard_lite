# Only the public-data/visualization steps need these packages.
# Core hardware inspection and supervisor unit tests use the standard library.
# Resolve on your machine, then save: python -m pip freeze > environment-lock.txt
huggingface_hub>=0.27,<2
numpy>=1.26,<3
matplotlib>=3.8,<4
opencv-python-headless>=4.9,<5
psutil>=5.9,<8
pyarrow>=16,<24
