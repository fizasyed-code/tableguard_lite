[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tableguard-lite-starter"
version = "0.1.0"
description = "CPU-first data inspection and bounded supervisor tests"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["tableguard*"]
